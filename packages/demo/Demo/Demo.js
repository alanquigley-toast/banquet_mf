import React from 'react'
import cx from 'classnames'

export const Demo = props => {
  return (
    <div
      className={cx('m-4 text-heading-3 text-default p-16 bg-gray-25 rounded')}
    >
      Demo
    </div>
  )
}

Demo.propTypes = {}
